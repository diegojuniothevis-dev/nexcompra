/* NexCompra Sync Guard v9.3.10 — sem srcdoc */
(() => {
  'use strict';
  const VERSION='9.3.10', MODULE='suite_localstorage';

  function textOf(v){try{return typeof v==='string'?v:JSON.stringify(v)}catch(_){return String(v??'')}}
  function hash(v){const s=textOf(v);let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0).toString(16)+':'+s.length}
  function payloadStats(v){const raw=textOf(v);let meaningful=0,records=0;const visit=(x,d=0)=>{if(d>7||x==null)return;if(Array.isArray(x)){if(x.length){meaningful+=2;records+=x.length}for(const y of x.slice(0,500))visit(y,d+1);return}if(typeof x==='object'){const ks=Object.keys(x);if(ks.length)meaningful++;for(const k of ks.slice(0,500))visit(x[k],d+1);return}if(typeof x==='number'){if(x!==0){meaningful++;records++}return}if(typeof x==='boolean'){if(x)meaningful++;return}if(typeof x==='string'){const s=x.trim();if(!s||/^(?:\[\]|\{\}|null|undefined|false|0)$/i.test(s))return;if((s[0]==='['&&s.endsWith(']'))||(s[0]==='{'&&s.endsWith('}'))){try{visit(JSON.parse(s),d+1);return}catch(_){}}meaningful++}};try{visit(v)}catch(_){}return{bytes:raw.length,meaningful,records}}
  function suspiciousEmpty(outgoing,remote){const o=payloadStats(outgoing),r=payloadStats(remote);if(r.bytes<1500)return false;return o.bytes<r.bytes*.38||(r.records>=20&&o.records<=Math.max(2,Math.floor(r.records*.08))&&r.meaningful>=20&&o.meaningful<=Math.max(3,Math.floor(r.meaningful*.10)))}

  function install(target){
    try{
      if(!target||target.__NX_SYNC_GUARD_INSTALLED__===VERSION)return true;
      if(target.__NX_SYNC_GUARD_NATIVE_FETCH__) target.fetch=target.__NX_SYNC_GUARD_NATIVE_FETCH__;
      const nativeFetch=target.fetch.bind(target);target.__NX_SYNC_GUARD_NATIVE_FETCH__=nativeFetch;target.__NX_SYNC_GUARD_INSTALLED__=VERSION;
      const states=new Map(),stateFor=id=>{const k=String(id||'');if(!states.has(k))states.set(k,{restored:false,lastKnownUpdatedAt:'',lastRemoteHash:'',lastPostedHash:'',blocked:0,skipped:0,emptyBlocked:0});return states.get(k)};
      const bodyOf=async req=>{try{const t=await req.clone().text();return t?JSON.parse(t):null}catch(_){return null}},rowOf=b=>Array.isArray(b)?b[0]||null:(b&&typeof b==='object'?b:null),ok=reason=>new target.Response('[]',{status:200,headers:{'Content-Type':'application/json','X-NexCompra-Sync-Guard':reason||'1'}}),newer=(a,b)=>{if(!a||!b)return false;const x=Date.parse(a),y=Date.parse(b);return Number.isFinite(x)&&Number.isFinite(y)&&x>y+500};
      async function remoteRow(req,cid){try{const u=new target.URL(req.url);u.search='';u.searchParams.set('select','payload,updated_at');u.searchParams.set('company_id','eq.'+cid);u.searchParams.set('module','eq.'+MODULE);u.searchParams.set('limit','1');const headers=new target.Headers(req.headers);headers.delete('content-type');const r=await nativeFetch(u.toString(),{method:'GET',headers,cache:'no-store'});if(!r.ok)return null;const j=await r.json().catch(()=>[]);return Array.isArray(j)?j[0]||null:j}catch(_){return null}}
      target.fetch=async function(input,init){let req;try{req=new target.Request(input,init)}catch(_){return nativeFetch(input,init)}let u;try{u=new target.URL(req.url,target.location.href)}catch(_){return nativeFetch(req)}if(!/\/rest\/v1\/nx_app_state$/i.test(u.pathname))return nativeFetch(req);const method=(req.method||'GET').toUpperCase(),moduleEq=u.searchParams.get('module');if(method==='GET'&&moduleEq==='eq.'+MODULE){const cid=(u.searchParams.get('company_id')||'').replace(/^eq\./,'');if(u.searchParams.has('select')&&!u.searchParams.get('select').includes('updated_at'))u.searchParams.set('select',u.searchParams.get('select')+',updated_at');const r=await nativeFetch(new target.Request(u.toString(),req));try{const j=await r.clone().json(),row=Array.isArray(j)?j[0]:j,st=stateFor(cid);st.restored=true;if(row){st.lastKnownUpdatedAt=row.updated_at||'';st.lastRemoteHash=hash(row.payload)}}catch(_){stateFor(cid).restored=true}return r}const upsert=method==='POST'&&u.searchParams.get('on_conflict')==='company_id,module';if(!upsert)return nativeFetch(req);const body=await bodyOf(req),row=rowOf(body);if(!row||row.module!==MODULE||!row.company_id)return nativeFetch(req);const cid=String(row.company_id),st=stateFor(cid),outHash=hash(row.payload),remote=await remoteRow(req,cid);if(!st.restored){if(remote){st.lastKnownUpdatedAt=remote.updated_at||'';st.lastRemoteHash=hash(remote.payload);st.blocked++;return ok('initial-restore')}st.restored=true}if(remote){const rh=hash(remote.payload),ra=remote.updated_at||'';if(rh!==outHash&&suspiciousEmpty(row.payload,remote.payload)){st.lastKnownUpdatedAt=ra;st.lastRemoteHash=rh;st.blocked++;st.emptyBlocked++;console.error('[NexCompra V9.3.10] estado vazio bloqueado; base remota preservada',cid);return ok('empty-state-blocked')}if(newer(ra,st.lastKnownUpdatedAt)&&rh!==outHash){st.lastKnownUpdatedAt=ra;st.lastRemoteHash=rh;st.blocked++;return ok('stale-session')}if(rh===outHash){st.lastKnownUpdatedAt=ra;st.lastRemoteHash=rh;st.skipped++;return ok('unchanged')}if(!st.lastKnownUpdatedAt)st.lastKnownUpdatedAt=ra}if(outHash===st.lastPostedHash||outHash===st.lastRemoteHash){st.skipped++;return ok('duplicate')}const response=await nativeFetch(req);if(response.ok){st.lastPostedHash=outHash;st.lastRemoteHash=outHash;remoteRow(req,cid).then(x=>{if(x){st.lastKnownUpdatedAt=x.updated_at||st.lastKnownUpdatedAt;st.lastRemoteHash=hash(x.payload)}}).catch(()=>{})}return response};
      target.NX_SYNC_GUARD={version:VERSION,states,payloadStats,status(){return[...states.entries()].map(([companyId,s])=>({companyId,...s}))}};
      console.info('[NexCompra Sync Guard] V'+VERSION+' ativo.');return true;
    }catch(e){console.warn('[NexCompra Sync Guard] falha ao instalar',e);return false}
  }

  install(window);
  try{if(parent!==window){parent.__NX_INSTALL_SYNC_GUARD__=install;parent.__NX_SYNC_GUARD_VERSION__=VERSION}}catch(_){}
})();
